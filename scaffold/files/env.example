# Local development database (docker compose up -d).
# In Graphed cloud, DATABASE_URL is injected from databases.primary — see graphed.yaml.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
